import React from 'react';
import ReactDOM from 'react-dom'; //DOM
import App from './App';

//Renderizando o componente App e jogando o seu valor para div root
ReactDOM.render(<App />, document.getElementById('root') );
